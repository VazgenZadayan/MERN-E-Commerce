import { makeStyles } from '@mui/styles';

export default makeStyles({
  container: {
    height: '100vh',
    paddingTop: '100px',
  },
  formController: {
    background: 'rgb(145 145 145 / 21%)',
  },
})