import { makeStyles } from '@mui/styles';

export default makeStyles({
  container: {
    minHeight: '78vh',
    maxHeight: '100vh',
  },
  isEmpty: {
    background: 'rgb(18,18,18)',
    width: 'fit-content',
    padding: '40px',
  },
  tableContainer: {
    maxHeight: '70vh',
    overflow: 'auto',
  },
});
